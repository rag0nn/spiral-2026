---
trigger: always_on
---

Write docs with 'Turkish'

Don't add "=" or like long "-" to comment rows, just comment content like title.

Don't test all addings without user asked to you. Just do what user wants.

Use conda env sentiment for bash.

When adding code, be simple as you can, code could be readable easily

If user orders is affects 2-4 code files, add abstracted chart to report which shows connections between.

After done all changes add or change project_summary.md 